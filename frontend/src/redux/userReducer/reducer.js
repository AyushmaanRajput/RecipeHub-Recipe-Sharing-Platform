import {} from "./actionTypes";

const initState = {
  isLoading: false,
  isError: false,
};

export const reducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
